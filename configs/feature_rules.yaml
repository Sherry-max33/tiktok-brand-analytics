# =============================================================================
# feature_rules.yaml — Feature engineering rules (keywords & thresholds)
# =============================================================================
# Matching / regex logic lives in src/tiktok_brand/etl/*.py
# CTA regex patterns are in src/tiktok_brand/etl/cta_rules.py (not here).
# Product/style taxonomy maps are in configs/taxonomy.yaml (not here).
#
# Index (top-level keys)
# ----------------------
# engagement_weights      Weighted engagement: like/comment/share/collect →
#                         weighted_engagement_count / rate / brand_relative_index
# content_type_rules      Caption → content_type (P0–P6 classification_priority:
#                         social_viral → … → community_collab). strong / weak /
#                         require_pairs / bts_context
# creator_type_keywords   author_signature → creator_type
#                         (sports / lifestyle / fashion / beauty)
# creator_tier_thresholds author_follower_count → creator_tier
#                         (nano / micro / mid / macro / mega)
# trending_audio          Sample-relative is_sample_trending_audio via music_id
#                         frequency (top_percentile + min_video_count)
# sentiment               Language detect + VADER; non-EN optional MT → vader_via_mt
#                         (en_confidence_threshold, mt_enabled, mt_min_chars)
# text_embedding          Multilingual Sentence-BERT on embedding_text (no MT)
#                         (enabled, model_name, min_chars, batch_size)
# visual_embedding        CLIP on cover_url/cover_path (enabled, model_name, batch_size)
# appearance_type         CV frame-ratio thresholds when detector ratios exist
# =============================================================================

engagement_weights:
  like: 0.10
  comment: 0.25
  share: 0.30
  collect: 0.35   # Apify collectCount → raw collect_count (legacy CSV: save_count)

# content_type: classification_priority P0–P6 (match order, not content value ranking)
content_type_rules:
  social_viral:
    strong:
      - ootd
      - grwm
      - unboxing
      - behind the scenes
      - tutorial
    weak:
      - trend
      - trending
      - viral
      - meme
      - pov
      - challenge
      - join the challenge
      - trend alert
      - tiktok trend
      - viral trend
      - capcut
      - capcut template
      - use this template
      - duet
      - duet this
      - stitch
      - stitch this
      - sound on
      - use this sound
      - wait for it
      - tell me without telling me
  official_campaign:
    strong:
      - official campaign
      - new collection
      - latest collection
      - campaign film
      - limited edition
      - new arrival
      - new arrivals
    weak:
      - official
      - officially
      - campaign
      - launch
      - launching
      - new launch
      - just launched
      - introducing
      - meet the
      - collection
      - new season
      - spring collection
      - summer collection
      - fall collection
      - winter collection
      - available now
      - coming soon
      - now available
      - exclusive
      - the new
      - debut
      - premiere
      - unveiling
      - reveal
    require_pairs:
      - [official, collection]
      - [new, launch]
  story_heritage:
    strong:
      - behind the scenes
      - behind-the-scenes
      - making of
      - the making of
      - our story
      - brand history
    weak:
      - heritage
      - legacy
      - history
      - our history
      - since
      - founded
      - established
      - origin
      - origins
      - archive
      - from the archive
      - archival
      - iconic
      - icon
      - anniversary
      - milestone
      - tradition
      - timeless
      - bts
      - craftsmanship
      - handcrafted
      - workshop
      - factory
      - design process
      - how it's made
      - how it’s made
    bts_context:
      - behind the scenes
      - behind-the-scenes
      - making of
      - on set
      - workshop
      - factory
  tutorial_utility:
    strong:
      - tutorial
      - how to
      - how-to
      - step by step
      - step-by-step
      - how to style
      - how to wear
      - how to use
      - how to clean
    weak:
      - tips
      - tips and tricks
      - pro tip
      - quick tip
      - hack
      - life hack
      - guide
      - explained
      - explainer
      - learn how
      - ways to
      - dos and don'ts
      - do's and don'ts
      - care guide
      - size guide
      - styling tips
      - training tips
      - workout tips
      - lace tutorial
  shopping_haul:
    strong:
      - haul
      - shopping haul
      - try-on haul
      - try on haul
      - unboxing
      - unbox
      - unboxed
      - product review
      - honest review
    weak:
      - review
      - first impression
      - first impressions
      - reaction
      - what i bought
      - what i ordered
      - what i got
      - shopping finds
      - recent purchases
      - pickup
      - pickups
      - cop or drop
      - worth it
      - must-have
      - must have
      - recommend
      - recommendation
      - rating
      - restock
      - back in stock
  vibe_ootd:
    strong:
      - ootd
      - outfit of the day
      - grwm
      - get ready with me
      - fit check
      - fitcheck
      - outfit check
      - outfit inspo
      - style inspo
      - fashion inspo
      - look inspo
      - outfit inspiration
    weak:
      - styling
      - styled
      - ways to wear
      - what i wore
      - today's fit
      - today’s fit
      - daily fit
      - street style
      - streetwear
      - lookbook
      - get the look
      - complete the look
      - day in the life
      - aesthetic
      - vibes
  community_collab:
    strong:
      - in collaboration with
      - in partnership with
      - co-created with
      - co-designed with
      - brand ambassador
      - athlete partnership
      - artist collaboration
      - capsule collection
    weak:
      - collab
      - collaboration
      - collaborates with
      - partnership
      - partnered with
      - partnering with
      - team up
      - teamed up
      - teaming up
      - join forces
      - together with
      - co-created
      - co-designed
      - guest designer
      - collab collection
      - limited collaboration
      - ambassador
      - creator partnership
      - community
      - our community
      - local community
      - giving back
      - charity
      - fundraiser
      - nonprofit
      - social impact
      - community event
      - volunteer
    require_pairs:
      - [community, event]
      - [capsule, collection]

creator_type_keywords:
  sports: [sport, fitness, athlete, gym, running, training, workout]
  lifestyle: [lifestyle, life, daily, vlog, living, creator, content]
  fashion: [fashion, style, outfit, ootd, wear, streetwear, sneaker]
  beauty: [beauty, makeup, skincare, cosmetic, glam]

creator_tier_thresholds:
  nano: 10000
  micro: 100000
  mid: 1000000
  macro: 10000000
  # mega: >= macro threshold

trending_audio:
  top_percentile: 0.9
  min_video_count: 3

# Sentiment: language detect + VADER; non-EN optionally via MT → vader_via_mt
sentiment:
  en_confidence_threshold: 0.80   # caption_lang==en and conf >= this → vader_en
  mt_enabled: true                # false → non-EN stays not_scored_non_en / pending
  mt_min_chars: 8                 # skip MT if cleaned text shorter than this
  # Provider: deep-translator GoogleTranslator (network). Inject translate_fn in tests.

# Text embeddings: multilingual Sentence-BERT (no MT; encode original embedding_text)
text_embedding:
  enabled: true
  model_name: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
  min_chars: 3
  batch_size: 32

# Visual embeddings: assemble TikTok URL → download MP4 → 4 frames @ 20/40/60/80%
visual_embedding:
  enabled: true
  model_name: sentence-transformers/clip-ViT-B-32
  batch_size: 16
  max_frames: 4
  frame_fractions: [0.2, 0.4, 0.6, 0.8]
  frames_dir: data/processed/frames
  download_video: false          # true or VISUAL_DOWNLOAD=1 → yt-dlp download then extract
  download_max_height: 480       # cap resolution (480p enough for frames/CLIP; avoid HD)
  # Optional: chrome|safari|firefox — or env YTDLP_COOKIES_FROM_BROWSER / enrich --cookies-from-browser
  cookies_from_browser: null

appearance_type:
  # Used when person/product frame ratios are supplied by a future CV step
  person_ratio: 0.6
  product_only_ratio: 0.6
  mixed_min_ratio: 0.2
  other_ratio: 0.6
